# 安全与运行边界

本项目是单用户本机 Demo，不是可直接开放到公网的 SaaS。

## 本地运行
- 服务绑定 127.0.0.1。不要直接改为 0.0.0.0 或通过公网隧道分享无鉴权服务。
- 没有账号、租户隔离或企业审计。只使用合成需求，不输入客户敏感资料。
- 任务与图片保存在 backend/.runtime，输入草稿可能保存在浏览器本地存储；公开源码不包含运行记录。
- 仅支持单进程，多 worker 不共享内存锁，无法保证一致性。

## 密钥与外部请求
- 密钥只填写 backend/.env，不写进源码、浏览器字段、截图或 Issue。
- DeepSeek 模式发送本轮输入与相关上下文；图片模式发送确认后的提示词及所选参考图。
- MCP 子进程不继承模型密钥；工具名及参数由服务端约束。
- 图像调用可能收费，需要独立授权，不自动重试。幂等防止本地重复提交，不保证跨故障的服务商 exactly-once。
- 超时或中断不代表服务商没有处理或计费。

## 公开分享
提交前检查文件清单。不要公开环境文件、历史记录、客户资料和生成缓存。交付 ZIP 可能含用户输入或提示词，发送前也需要检查。

发现密钥泄露时由账号所有者在服务商端撤销并替换；删除当前文件不能清除 Git 历史中的泄露。不要在公开 Issue 发布密钥、完整请求日志或敏感数据。

## 企业化前置条件
鉴权、租户存储隔离、密钥托管、费用限额、任务队列、服务认证、安全评测、数据授权与删除策略，以及写入工具的人工审批和回执。模型输出与素材权利仍需业务审核。
